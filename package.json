{
  "name": "smalls-smash-club",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "preview:open": "vite build && vite preview --open",
    "logo:strip-checker": "node scripts/strip-logo-checker.mjs",
    "logo:trim": "node scripts/trim-logo.mjs",
    "favicon": "node scripts/generate-favicon.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
